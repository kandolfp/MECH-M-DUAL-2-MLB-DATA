This project is used in the lecture _Machine Learning in Industrial Image Processing_ for demonstrating some key aspects of persisting models and data in data science applications.

It is a `pdm` project written in Python 3.12 with `sklearn` and `dvc` as additional components. 